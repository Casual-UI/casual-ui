---
title: 表单相关
hooksAPI:
  - components/form/useFormContext
  - components/form/useFormItemContext
---

### `useFromContext`

对于每个`CForm`表单组件，都具有一个`CFormContext`，该上下文可以通过`useFormContext`方法获取。该方法的签名如下：

### `useFormItemContext`

对于每个`CFormItem`表单项组件，都具有一个`CFormItemContext`，该上下文可以通过`useFormItemContext`方法获取。该方法的签名如下：
